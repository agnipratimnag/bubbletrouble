/*class Healthbar
{
private:
    Rectangle health; // The healthbar
    Color color;    // The color of the shooter

public:
    Healthbar(double position_x, double position_y, double height=30, double width=18, Color color_=COLOR(0, 255, 0))
    {
        // Healthbar constructor
        color = color_;

        // body (represented by a rectangle)
        health = Rectangle(position_x, position_y, width, height);
        health.setColor(color);
        health.setFill(true);
    }
*/
